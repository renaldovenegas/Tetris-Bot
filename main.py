from board import Board
from piece import *
import random

a = 4
depth = 5

if __name__ == "__main__":
    board = Board(20, 10)
    pieces = [PieceL(), PieceJ(), PieceI(), PieceO()]
    queue = []
    # L = PieceL()
    # J = PieceJ()
    # J.rotate(2)
    # I = PieceI()
    # sideI = PieceI()
    # sideI.rotate(1)
    # O = PieceO()
    # board.place_piece(L, 0)
    # board.display()
    # board.place_piece(L, 2)
    # board.display()
    # board.place_piece(L, 4)
    # board.display()
    # board.place_piece(L, 6)
    # board.display()
    # board.place_piece(L, 8)
    # board.display()
    # board.place_piece(J, 1)
    # board.display()
    # board.place_piece(J, 3)
    # board.display()
    # board.place_piece(J, 5)
    # board.display()
    # board.place_piece(J, 7)
    # board.display()
    # L.rotate(1)
    # board.place_piece(L, 0)
    # board.display()
    # board.place_piece(sideI, 3)
    # board.display()
    # board.place_piece(O, 7)
    # board.display()
    # board.place_piece(I, 9)
    # board.display()
    print("done!")